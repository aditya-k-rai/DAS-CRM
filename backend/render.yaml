services:
  - type: web
    name: nexcrm-backend
    name: dascrm-backend
    env: node
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        fromDatabase:
          name: dascrm-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

databases:
  - name: dascrm-db
    plan: free
    databaseName: dascrm
    user: postgres
